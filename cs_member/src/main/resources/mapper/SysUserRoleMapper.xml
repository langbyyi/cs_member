<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cloud.hilang.cs_member.mapper.SysUserRoleMapper">

    <!-- 根据用户ID查询角色ID列表 -->
    <select id="selectRoleIdsByUserId" parameterType="Long" resultType="Long">
        SELECT
            role_id
        FROM
            sys_user_role
        WHERE
            user_id = #{userId}
    </select>

    <!-- 根据用户ID删除角色关联 -->
    <delete id="deleteByUserId" parameterType="Long">
        DELETE FROM
            sys_user_role
        WHERE
            user_id = #{userId}
    </delete>

    <!-- 根据用户ID和角色ID删除角色关联 -->
    <delete id="deleteByUserIdAndRoleId" parameterType="Map">
        DELETE FROM
            sys_user_role
        WHERE
            user_id = #{userId}
            AND role_id = #{roleId}
    </delete>

    <!-- 插入用户角色关联 -->
    <insert id="insertUserRole" parameterType="Map">
        INSERT INTO
            sys_user_role (user_id, role_id)
        VALUES
            (#{userId}, #{roleId})
    </insert>

    <!-- 根据用户ID查找角色列表 -->
    <select id="findRolesByUserId" parameterType="Long" resultType="cloud.hilang.cs_member.entity.SysRole">
        SELECT
            r.id,
            r.role_code,
            r.role_name,
            r.role_type,
            r.description
        FROM
            sys_role r
        INNER JOIN
            sys_user_role ur ON r.id = ur.role_id
        WHERE
            ur.user_id = #{userId}
    </select>
    
    <!-- 根据用户ID查询用户角色关联 -->
    <select id="selectByUserId" parameterType="Long" resultType="cloud.hilang.cs_member.entity.SysUserRole">
        SELECT
            id,
            user_id,
            role_id
        FROM
            sys_user_role
        WHERE
            user_id = #{userId}
        LIMIT 1
    </select>

</mapper>