<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cloud.hilang.cs_member.mapper.SysRolePermissionMapper">

    <!-- 根据角色ID查询权限ID列表 -->
    <select id="selectPermissionIdsByRoleId" parameterType="Long" resultType="Long">
        SELECT
            permission_id
        FROM
            sys_role_permission
        WHERE
            role_id = #{roleId}
    </select>

    <!-- 根据角色ID删除权限关联 -->
    <delete id="deleteByRoleId" parameterType="Long">
        DELETE FROM
            sys_role_permission
        WHERE
            role_id = #{roleId}
    </delete>

    <!-- 批量插入角色权限关联 -->
    <insert id="batchInsertRolePermissions" parameterType="List">
        INSERT INTO
            sys_role_permission (role_id, permission_id, created_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.roleId}, #{item.permissionId}, NOW())
        </foreach>
    </insert>

    <!-- 根据角色ID列表查询权限ID列表 -->
    <select id="selectPermissionIdsByRoleIds" parameterType="List" resultType="Long">
        SELECT DISTINCT
            permission_id
        FROM
            sys_role_permission
        WHERE
            role_id IN
            <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
                #{roleId}
            </foreach>
    </select>

</mapper>